# Local dev: the web app talks to the API on localhost:4000.
# In production (GitHub Pages), VITE_API_URL is injected by the Deploy Web
# workflow from the repo variable `VITE_API_URL` (default: the Render API URL).
VITE_API_URL=http://localhost:4000
